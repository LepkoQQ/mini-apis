networks:
  traefik:
    external: true

services:
  web:
    command: !reset null
    container_name: mini-apis
    volumes: !override
      - ./data:/app/data:rw
    environment: !override
      - VALSTORE_DB_PATH=/app/data/valstore.db
      - VALSTORE_API_KEY=${VALSTORE_API_KEY}
    networks:
      - traefik
    labels:
      - traefik.enable=true
      - traefik.http.routers.mini-apis-http.rule=Host(`${DOMAIN_NAME}`)
      - traefik.http.routers.mini-apis-http.entrypoints=web
      - traefik.http.routers.mini-apis-https.rule=Host(`${DOMAIN_NAME}`)
      - traefik.http.routers.mini-apis-https.entrypoints=websecure
      - traefik.http.routers.mini-apis-https.tls=true
      - traefik.http.routers.mini-apis-https.tls.certresolver=le
    restart: unless-stopped
